const Navbar = () => {
    // creates the navbar container and adds default styles

}

export const changeNavLinks = () => {
    // depending on page will display different nav links
    // ie:logged in vs not logged in
}

export default Navbar